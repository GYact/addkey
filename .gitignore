# Never commit plaintext secrets. Commit only the encrypted *.enc copies.
.env
.env.*
!.env.*.enc
!.env.enc
!.env.example
*.bak.*

# age private keys must never be committed
keys.txt
*.agekey

# OS noise
.DS_Store

# Local-only promo drafts (kept out of the public repo)
docs/sns-posts.txt
docs/zenn-article-draft.txt
